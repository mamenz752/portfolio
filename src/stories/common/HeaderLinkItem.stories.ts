import { HeaderLinkItem } from "@/components/common/components/HeaderLinkItem";
import { Meta } from "@storybook/react";

const meta: Meta<typeof HeaderLinkItem> = {
  component: HeaderLinkItem,
  title: "Common/LinkItem",
  tags: ["autodocs"],
};

export default meta;

export const LinkItem = {};
